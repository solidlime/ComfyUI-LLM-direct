# Pin the rootdir to tests/: without this, pytest walks up to the ComfyUI
# root pytest.ini and treats this node folder (which has __init__.py) as a
# package, importing __init__.py during collection — which requires
# ComfyUI-only deps (folder_paths, llama_cpp).
